package com.jobboard.entity.enums;

/** Miroir exact des valeurs autorisées par la contrainte CHECK sur applications.job_type. */
public enum JobType {
    CDI,
    CDD,
    INTERNSHIP,
    ALTERNANCE,
    FREELANCE
}
