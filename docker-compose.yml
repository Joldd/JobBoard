services:
  postgres:
    image: postgres:17-alpine
    container_name: jobboard-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${DB_NAME:-jobboard}
      POSTGRES_USER: ${DB_USERNAME:-jobboard}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-jobboard}
    ports:
      - "${DB_PORT:-5432}:5432"
    volumes:
      - jobboard-postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USERNAME:-jobboard} -d ${DB_NAME:-jobboard}"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  jobboard-postgres-data:
